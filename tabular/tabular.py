

# Generate the MDP: P,R- (see randMat.m)

# Compute the optimal-policy, e.g, using policy-iteration (see dp.m)
# policy-iteration: until policy not changes: run policy evaluation + new+policy = argmax Q

s

